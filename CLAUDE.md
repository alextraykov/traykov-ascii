# Claude Notes

Use `AGENTS.md` as the source of truth.
Do not add dependencies unless the user explicitly asks.
Keep `src/styles/global.css` as the single token/design-language file.
Before handoff, run `npm run verify` and `npm run build`.
